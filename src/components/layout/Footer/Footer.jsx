import styles from "./Footer.module.css";

function Footer() {
  return (
    <footer className={`${styles.footer} py-5 text-white`}>
        <nav className="container flex flex-row flex-wrap justify-between">
            
        </nav>
    </footer>
  );
}

export default Footer;